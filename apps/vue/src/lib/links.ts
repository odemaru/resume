// Ссылка ведёт на ту страницу Next, из которой собрана эта сборка,
// иначе переключатель уводил бы на резюме с другим текстом.
export const NEXT_URL = import.meta.env.VITE_NEXT_URL || '../frontend/';
